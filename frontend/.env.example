# Copy to .env (or .env.local) and adjust per environment.
# Never commit real .env files — see .gitignore.

# Base URL for the backend API. No backend exists yet (Phase P1-FE-01A),
# so this is a placeholder; it must be set once the backend is available.
VITE_API_BASE_URL=http://localhost:8000/api/v1
