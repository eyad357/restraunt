/**
 * Dashboard module public surface.
 *
 * Nothing needs to be re-exported today — routing integration happens via
 * routes.tsx (auto-discovered, see src/routes/moduleRoutes.ts). Exists as
 * the documented extension point per the module structure convention.
 */
export {};
